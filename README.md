# PortfolioBadaraSENE

## Modifications Récentes
- Amélioration du formulaire de contact : Remplacement du simple lien `mailto` par une soumission de formulaire (méthode POST) via le service public *FormSubmit* (`https://formsubmit.co`). 
- Ajout des attributs `name` sur les champs `<input>` pour permettre l'envoi correct des données. N'importe quel visiteur peut désormais envoyer un e-mail directement depuis le site web sans avoir de client de messagerie configuré.
- Correction du lien e-mail dans `index.html` : Remplacement de l'attribut `href="[senebabos1986@gmail.com]"` par un lien fonctionnel `href="mailto:senebabos1986@gmail.com"`.
- Correction d'une erreur de frappe sur la balise `<form>` (précédemment écrite `<from>`).
- Ajout d'un footer (pied de page) contenant les mentions de copyright et le numéro de téléphone en bas de `index.html`.
- Modification de la couleur du texte du footer en blanc pour une meilleure visibilité (puis supprimé par la suite au profit du lien social).
- Ajout d'un bouton WhatsApp dans la section de présentation, avec un lien optimisé (`wa.me`) pour ouvrir directement la messagerie.